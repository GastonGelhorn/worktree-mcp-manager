[package]
name = "worktree-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
log = "0.4"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
